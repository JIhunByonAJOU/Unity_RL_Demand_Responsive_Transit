# README Assets

The root README currently uses these images:

```text
scene1.png
game1.png
passenger-request.png
drt-bus-status.png
```

Keep the filenames unchanged unless the image paths in the root `README.md` are updated together.
